{
  "name": "mermaid2aichat",
  "version": "1.0.0",
  "private": true,
  "description": "Mermaid2AIChat — 通过 MCP 协议将可视化流程图编辑器与 AI 对话无缝集成，配套 MCP 服务和 SKILL",
  "type": "module",
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "test:e2e": "playwright test",
    "typecheck": "pnpm -r run typecheck",
    "lint": "pnpm -r run lint"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.0",
    "typescript": "^5.5.0"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20.0.0"
  },
  "pnpm": {
    "overrides": {
      "esbuild": "^0.21.5"
    }
  }
}
