flowchart TB
    subgraph Start["开始"]
        A[获取当前时间] --> B[收集用户初步想法]
    end

    subgraph L0["L0 初步想法（概念级）"]
        B --> C[识别设计决策点]
        C --> D{提供三套设计方案}
        D --> E[AskUserQuestion 用户选择]
        E --> F[记录决策到需求规格说明书]
        F --> G[推荐L1模块划分]
        G --> H[询问用户确认]
    end

    subgraph L1["L1 功能模块级（宏观级-顺序执行）"]
        H --> I1[模块A设计]
        I1 --> I2[模块B设计]
        I2 --> I3[模块C设计]
        I3 --> IN[模块N设计]

        subgraph ModuleA["模块A完整流程"]
            I1 --> MA1[创建模块文档]
            MA1 --> MA2[L1设计分析]
            MA2 --> MA3{三套设计方案}
            MA3 --> MA4[AskUserQuestion选择]
            MA4 --> MA5[记录决策]
            MA5 --> MA6[用户交互设计]
            MA6 --> MA7[数据流转设计]
            MA7 --> MA8[推荐L2划分]
            MA8 --> MA9[写入文档+总结检验]
        end

        subgraph ModuleB["模块B完整流程"]
            I2 --> MB1[创建模块文档]
            MB1 --> MB2[L1设计分析]
            MB2 --> MB3{三套设计方案}
            MB3 --> MB4[AskUserQuestion选择]
            MB4 --> MB5[记录决策]
            MB5 --> MB6[用户交互设计]
            MB6 --> MB7[数据流转设计]
            MB7 --> MB8[推荐L2划分]
            MB8 --> MB9[写入文档+总结检验]
        end
    end

    subgraph L2_L3["L2-L4 递归细化（中观/微观/原子级）"]
        IN --> J[L2子功能级-顺序执行]
        J --> K[L3组件级-顺序执行]
        K --> LL[L4实现级-可选]
        LL --> LM[L5+持续细化-可选]
    end

    subgraph Validate["验证流程（使用子Agent）"]
        LM --> N[准备验证任务]
        P -->|是| Q[进入文档集生成]
        P -->|否| R[修正错误]
        R --> S[额外验证流程]
    end

    subgraph DocGen["标准设计文档集生成"]
        Q --> T1[需求类文档]
        T1 --> T2[设计类文档]
        T2 --> T3[测试类文档]
        T3 --> T4[部署运维文档]
        T4 --> T5[项目管理文档]
        T5 --> T6[其他文档]
    end

    subgraph FinalValidate["最终批量验证（使用子Agent）"]
        T6 --> U[批量验证子Agent]
        U --> V{全部通过?}
        V -->|是| W[完成✓]
        V -->|否| X[修正+额外验证]
        X --> U
    end

    style Start fill:#e1f5fe
    style L0 fill:#fff3e0
    style L1 fill:#f3e5f5
    style L2_L3 fill:#e8f5e9
    style Validate fill:#ffebee
    style DocGen fill:#e0f2f1
    style FinalValidate fill:#fce4ec